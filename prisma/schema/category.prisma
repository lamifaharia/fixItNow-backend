model Category {
    id String @default(uuid()) @unique
    name String @unique
    description String?
    image String?

    services Service[]

    createdAt DateTime @default(now())
    updateAt DateTime @updatedAt
}
